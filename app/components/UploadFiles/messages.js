/*
 * UploadFiles Messages
 *
 * This contains all the text for the UploadFiles component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.UploadFiles";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the UploadFiles component!"
  }
});
